Download and install python silently
curl -O https://www.python.org/ftp/python/3.13.7/python-3.13.7-amd64.exe

Open new Command Prompt to verify (important!)
py --version



cd C:\Users\Leoncio\Desktop\Recipe Batching

python.exe -m pip install --upgrade pip


del /q dist\* 2>nul

del /q build\* 2>nul

del /q *.spec 2>nul

python -m venv venv

venv\Scripts\activate


pip install pyinstaller pyserial pandas openpyxl pywin32 pillow

pyinstaller batching_app.spec

pyinstaller --onefile --windowed ^
--icon="C:\Users\Leoncio\Desktop\Recipe Batching\assets\icon.ico" ^
--manifest "admin.manifest" ^
BatchingApp.py


i  like the omitted methods included explicitly or exported as a .py file.

Password:789123

Create the application to make the application always run as administrator
